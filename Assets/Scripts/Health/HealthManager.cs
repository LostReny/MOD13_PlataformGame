using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Singleton;
using System.Drawing;

public class HealthManager : HealthBase
{
    /*
    public SOInt playerLife;

    private void Awake()
    {
        if (Instance == null)
            Instance = this;
        else
            Destroy(gameObject);
    }

    private void Update()
    {

    }

    private void Start()
    {
        Reset();
    }

    private void Reset()
    {
        

    }

    public void ChangeLife(int life)
    {
        life = _currentLife;

        playerLife = life.value.ToString();    
    }
    */
}
